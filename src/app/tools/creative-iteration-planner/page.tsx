import { CreativeIterationPlannerClient } from "./client";

export const metadata = {
  title: "Creative Iteration Planner — ShapersOS",
};

export default function CreativeIterationPlannerPage() {
  return <CreativeIterationPlannerClient />;
}
