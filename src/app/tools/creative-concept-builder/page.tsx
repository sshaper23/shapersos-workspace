import { CreativeConceptBuilderClient } from "./client";

export const metadata = {
  title: "Creative Concept Builder — ShapersOS",
};

export default function CreativeConceptBuilderPage() {
  return <CreativeConceptBuilderClient />;
}
