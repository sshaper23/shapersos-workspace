export const defaultBusinessContexts = [
  { id: "none", name: "No Business" },
] as const;

export const defaultOffers = [
  { id: "none", name: "No Offer" },
] as const;

export const defaultStyleGuides = [
  { id: "none", name: "No Style" },
] as const;
